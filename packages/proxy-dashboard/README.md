# `@software-sheba/proxy-dashboard`

This app is frontend part of [proxy-api](../proxy-api). for run this app just type

```bash
yarn dev:proxy-dashboard
```

but this app full depend on api, so first serve the api app

```bash
yarn dev:proxy-api
```
